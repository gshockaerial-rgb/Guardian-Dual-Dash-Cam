# Guardian Dual Dash Cam website

Upload all files and folders in this directory to the root of your GitHub repository. In Netlify, use no build command and set the publish directory to `.`.

Before publishing, replace the support-email note in `contact.html` with your actual support email. The store buttons currently open verified store searches because exact listing URLs were not supplied. Replace them with your direct listing URLs when available.
